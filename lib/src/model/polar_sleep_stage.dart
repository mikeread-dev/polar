/// Represents different stages of sleep as detected by Polar devices
enum PolarSleepStage {
  /// Awake stage during sleep
  awake,
  
  /// Light sleep stage
  light,
  
  /// Deep sleep stage
  deep,
  
  /// REM (Rapid Eye Movement) sleep stage
  rem,
}